/**
 * GitHub Pages and site identity.
 * Change `repositoryName` if this project is published under a different repo.
 */
export const siteConfig = {
  githubUsername: "GangaAuji",
  repositoryName: "PlainPortfolio",
};
